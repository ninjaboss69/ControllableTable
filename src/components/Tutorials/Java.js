import React from "react";

const Java = () => {
  return (
    <div className="pl-3 bg-primary h-[100vh]  bg-primary">
      <h2 className="text-link text-gray-300 leading-10 px-3">
        Java is hard to understand. It is kinda low but not that low to the
        level where you have to manage memory. It kinda high but not that high
        to the level where you have to evaluate the mathematical string
        yourself.
      </h2>
    </div>
  );
};

export default Java;
