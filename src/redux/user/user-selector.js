export const selectCurrentUsers = (state) => state.users.currentUsers;

export const selectCloseKeys = (state) => state.users.closeKeys;

export const selectFilterUsers = (state) => state.users.filterUsers;

export const selectOrderKeys = (state) => state.users.orderKeys;
